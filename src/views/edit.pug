extends base.pug

block content 
    h4 Change Title of Video 
    form(method="POST")
        input(name="title" required placeholder="Video Title",value=video.title) 
        input(value="Save",type="submit")